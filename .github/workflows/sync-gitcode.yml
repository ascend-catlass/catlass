name: Sync GitCode master and tags

on:
  schedule:
    # 02:00 Asia/Shanghai (18:00 UTC)
    - cron: "0 18 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    uses: ascend-catlass/actions/.github/workflows/sync-gitcode.yml@main
